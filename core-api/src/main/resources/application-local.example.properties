# Copy to application-local.properties and set your real password.
# application-local.properties is in .gitignore (not committed).
# Run with: .\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=local

# spring.datasource.url=jdbc:postgresql://localhost:5432/ifyousayyes
# spring.datasource.username=postgres
# spring.datasource.password=YOUR_POSTGRES_PASSWORD
